<%@ jet
	package="com.ebmwebsourcing.petals.services.bpel.generated"
	class="ProcessWsdl"
	skeleton="generator.skeleton" %>
<% String processName = (String) argument; %>
<?xml version="1.0"?>
<definitions name="<%= processName %>Definition"
        targetNamespace="http://petals.ow2.org/bpel/<%= processName %>/"
        xmlns:tns="http://petals.ow2.org/bpel/<%= processName %>/"
        xmlns:plnk="http://docs.oasis-open.org/wsbpel/2.0/plnktype"
        xmlns="http://schemas.xmlsoap.org/wsdl/"
        xmlns:soap="http://schemas.xmlsoap.org/wsdl/soap/">
 
<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     TYPE DEFINITION - List of types participating in this BPEL process 
     The BPEL Designer will generate default request and response types
     but you can define or import any XML Schema type and use them as part 
     of the message types.
     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->    
    <types>
        <schema 
        		attributeFormDefault="unqualified" 
        		elementFormDefault="qualified" 
            	targetNamespace="http://petals.ow2.org/bpel/<%= processName %>/" 
            	xmlns="http://www.w3.org/2001/XMLSchema"
            	xmlns:petals="http://petals.ow2.org/bpel/<%= processName %>/">

			<element name="<%= processName %>Request" type="petals:<%= processName %>Request" />
            <complexType name="<%= processName %>Request">
                    <sequence>
                        <element name="input" type="string"/>
                    </sequence>
            </complexType>

			<element name="<%= processName %>Response" type="petals:<%= processName %>Response" />
            <complexType name="<%= processName %>Response">
                    <sequence>
                        <element name="result" type="string"/>
                    </sequence>
            </complexType>
        </schema>
    </types>

<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     MESSAGE TYPE DEFINITION - Definition of the message types used as 
     part of the port type definitions
     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->    
    <message name="<%= processName %>RequestMessage">
        <part name="payload" element="tns:<%= processName %>Request"/>
    </message>
    <message name="<%= processName %>ResponseMessage">
        <part name="payload" element="tns:<%= processName %>Response"/>
    </message>
    
<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     PORT TYPE DEFINITION - A port type groups a set of operations into
     a logical service unit.
     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->    

    <!-- portType implemented by the processName BPEL process -->
    <portType name="<%= processName %>PortType">
        <operation name="process">
            <input name="processInput" message="tns:<%= processName %>RequestMessage" />
            <output name="processOutput" message="tns:<%= processName %>ResponseMessage"/>
        </operation>
    </portType>


 <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     BINDING DEFINITION 
      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->    
  
 	<binding name="<%= processName %>Binding" type="tns:<%= processName %>PortType">
    	<soap:binding style="document"
 			transport="http://schemas.xmlsoap.org/soap/http" />
   		
   		<operation name="process">
			<soap:operation soapAction="http:petals.ow2.org/process" />
			<input name="processInput">
    			<soap:body use="literal" />
    		</input>
    		<output name="processOutput">
    			<soap:body use="literal" />
    		</output>
		</operation>
	</binding>
      
 <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     SERVICE DEFINITION 
     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->    
  
    <service name="<%= processName %>Service">
    	<port name="<%= processName %>Endpoint" binding="tns:<%= processName %>Binding">
    		<soap:address location="<%= processName %>Endpoint"></soap:address>
    	</port>
    </service>
    
</definitions>
